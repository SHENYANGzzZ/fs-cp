# 飞书文档爬虫 - 待办事项

## 项目目标
尽最大努力还原真实的飞书文档内容和格式

---

## 问题诊断 (2026-03-11)

### 🔴 严重问题

#### 1. 文字内容错误
- **OCR识别错误率高**：约 30% 的内容识别错误
  - `pbython` → `python`
  - `condacreate` → `conda create`
  - `pipInstall` → `pip install`
  - 中文字符间多余空格
- **界面元素混入**：导航栏、按钮等被识别为内容
- **URL被截断**：链接被换行分割

#### 2. 格式还原问题
- **标题层级错误**：`一、` `二、` 等标题没有正确识别
- **步骤编号丢失**：`第一步` `第二步` 格式没有保留
- **代码块边界不清**：代码和普通文本混在一起
- **表格乱序**：表格内容被识别为无序文本

#### 3. 图片处理问题
- **原文图片丢失**：文档中的图片没有提取
- **截图位置错误**：截图应该嵌入到对应内容位置，而不是全部放在末尾

---

## 改进计划

### 第一阶段：OCR识别优化 🔴 最高优先级

#### P0 - 必须完成

- [ ] **改进OCR识别质量**
  - **问题**：Tesseract.js 对中文识别效果差
  - **方案A**：使用更高质量的OCR引擎（如 PaddleOCR）
  - **方案B**：使用在线OCR服务（如百度OCR、腾讯OCR）
  - **方案C**：直接提取页面DOM内容（不使用OCR）

- [ ] **优化DOM内容提取**
  - **问题**：当前DOM提取内容不完整
  - **原因**：飞书文档懒加载机制
  - **方案**：
    - 增加滚动等待时间
    - 检测内容加载完成状态
    - 使用 MutationObserver 监听内容变化

- [ ] **修复代码块识别**
  - **问题**：代码命令被识别为普通文本
  - **方案**：
    - 识别 `conda` `pip` `export` 等命令前缀
    - 识别代码块容器元素
    - 保持代码块原始格式

### 第二阶段：格式还原优化 🔴 高优先级

#### P0 - 必须完成

- [ ] **标题层级识别**
  - 识别 `一、` `二、` `三、` 格式
  - 识别 `v 1.` `v 2.` 格式
  - 识别 `第X步` 格式

- [ ] **步骤编号保留**
  - 保留 `第一步` `第二步` 格式
  - 保留 `①` `②` `③` 格式
  - 保留 `1.` `2.` `3.` 格式

- [ ] **代码块格式化**
  - 正确识别代码块边界
  - 保持代码缩进
  - 使用等宽字体

### 第三阶段：图片处理优化 🟡 中优先级

#### P0 - 必须完成

- [ ] **提取原文图片**
  - **问题**：图片URL需要认证
  - **方案A**：实现登录功能
  - **方案B**：使用截图替代（当前方案）
  - **方案C**：手动提供Cookie

- [ ] **图片位置对应**
  - **问题**：截图全部放在文档末尾
  - **方案**：将截图嵌入到对应内容位置

### 第四阶段：表格识别 🟢 低优先级

#### P2 - 可选

- [ ] **表格识别**
  - **难点**：OCR对表格识别效果差
  - **方案**：尝试提取DOM中的表格元素

---

## 已完成 ✅

- [x] 项目初始化与运行
- [x] 添加滚动加载功能
- [x] 切换到手机模拟器模式
- [x] 实现截图+OCR提取方案
- [x] 移除浏览器日志显示
- [x] 性能优化（减少等待时间）
- [x] 滚动状态检测（到达底部自动停止）
- [x] 开发日志整理
- [x] 文本后处理模块（效果有限）
- [x] Word文档格式化（基础版）
- [x] 截图嵌入（位置不对）

---

## 技术方案参考

### 方案A：使用 PaddleOCR
```bash
# 安装 PaddleOCR
pip install paddlepaddle paddleocr

# Python 调用
from paddleocr import PaddleOCR
ocr = PaddleOCR(use_angle_cls=True, lang='ch')
result = ocr.ocr('screenshot.png')
```

### 方案B：使用百度OCR API
```javascript
// 调用百度OCR API
const axios = require('axios');
const result = await axios.post('https://aip.baidubce.com/rest/2.0/ocr/v1/general_basic', {
  image: base64Image,
});
```

### 方案C：优化DOM提取
```typescript
// 等待内容加载完成
await page.waitForFunction(() => {
  const content = document.querySelector('.wiki-content');
  return content && content.scrollHeight > 0;
}, { timeout: 60000 });

// 监听内容变化
await page.evaluate(() => {
  return new Promise((resolve) => {
    const observer = new MutationObserver(() => {
      // 检测内容是否稳定
    });
    observer.observe(document.body, { childList: true, subtree: true });
  });
});
```

---

## 下一步行动

1. **优先尝试方案C**：优化DOM内容提取，避免使用OCR
2. **如果DOM提取仍然不完整**：考虑使用更好的OCR引擎
3. **修复格式识别**：改进标题、步骤、代码块的识别规则
4. **优化图片处理**：将截图嵌入到正确位置

---

## 更新日志

- **2026-03-11 12:30**: 重新诊断问题，制定新的改进计划
  - 发现OCR识别错误率高（约30%）
  - 发现格式还原问题严重
  - 发现图片位置不对
  - 决定优先优化DOM内容提取
- **2026-03-11 12:00**: 完成第三阶段（图片处理优化）
- **2026-03-11 11:30**: 完成第二阶段（格式还原优化）
- **2026-03-11 10:45**: 完成第一阶段（内容清洗优化）
- **2026-03-11 09:50**: 创建TODO.md，规划四个阶段的改进任务
